import { LoadingState } from "@/shared/freamwork/components/wrappers/loading-state.wrapper"

export const LoadingAuthentification = () => {
    return (
        <LoadingState title="Hang tight! We're checking your account to make sure everything is secure." />
    )
}
