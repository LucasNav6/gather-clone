import { LoadingState } from "@/shared/freamwork/components/wrappers/loading-state.wrapper"

export const LoadingRooms = () => {
    return (
        <LoadingState title="Just a sec! We're loading the rooms you're part of so you can jump back in." />
    )
}